<?xml version="1.0" encoding="UTF-8"?>
<presentation>

<!-- ═══════════════════════════════════════════════════════════
     PART 1: INTRODUCTION (Slides 1-7)
     ═══════════════════════════════════════════════════════════ -->

<slide type="title">
  <title>DALI2</title>
  <subtitle>Multi-Agent System Framework Built on SWI-Prolog
Process-per-Agent Architecture  ·  Redis Star Topology  ·  Full DALI Compatibility

University of L'Aquila — DISIM</subtitle>
</slide>

<slide type="content">
  <title>Agenda</title>
  <bullets>
    <b>**Introduction** — What is DALI? What is DALI2?</b>
    <b>**Architecture** — Redis star topology, process-per-agent model</b>
    <b>**Language Fundamentals** — Agent declarations, suffixes, operators</b>
    <b>**Reactive Rules** — External events, actions, communication</b>
    <b>**Internal Events and Periodic Tasks** — Proactive behavior</b>
    <b>**Condition and Environment Rules** — Monitors, constraints</b>
    <b>**Goals and Memory** — Achieve/test goals, past events, export past</b>
    <b>**Communication** — Tell/told filtering, FIPA messages, proposals</b>
    <b>**Advanced Features** — Ontology, learning, AI Oracle, blackboard</b>
    <b>**Agent Lifecycle** — Cycle-based event loop</b>
    <b>**Examples** — Agriculture, emergency, showcase, distributed</b>
  </bullets>
</slide>

<slide type="content">
  <title>What is DALI?</title>
  <text>DALI (Distributed Active Logic-based Interactive) is an agent-oriented programming language originally developed on SICStus Prolog.</text>
  <bullets>
    <b>**Logic-based agents** — each agent is a Prolog program with reactive and proactive capabilities</b>
    <b>**BDI-inspired** — agents have Beliefs, react to events, pursue Goals</b>
    <b>**Event-driven** — external events (:>), internal events, condition-action rules</b>
    <b>**Communication** — agents send messages using Linda-like coordination</b>
    <b>**Past memory** — agents remember what happened and reason about history</b>
    <b>**FIPA-ACL support** — standard agent communication language</b>
    <b>**Original implementation** — ~20 source files, SICStus Prolog, TCP sockets, Python UI</b>
  </bullets>
</slide>

<slide type="content">
  <title>From DALI to DALI2</title>
  <text>DALI2 is the evolution of DALI — same language, modern architecture.</text>
  <bullets>
    <b>**Same syntax** — identical operators (:>, :&lt;, ~/, &lt;/, ?/, :~) and suffixes (E, I, A, N, P)</b>
    <b>**SWI-Prolog** — free, open-source, cross-platform (replaces SICStus)</b>
    <b>**8 source files** — down from ~20 in the original</b>
    <b>**Single-file multi-agent** — all agents in one .pl file</b>
    <b>**Redis communication** — replaces TCP sockets with pub/sub star topology</b>
    <b>**Integrated web UI** — dashboard, log viewer, agent inspector (replaces separate Python UI)</b>
    <b>**Docker-ready** — one command to run everything</b>
    <b>**New features** — periodic tasks, condition monitors, helpers, AI Oracle, blackboard</b>
  </bullets>
</slide>

<slide type="content">
  <title>Key Features</title>
  <bullets>
    <b>**Identical DALI syntax** — no prefix needed, same operators and suffixes</b>
    <b>**Process-per-agent** — each agent runs as a separate OS process (swipl)</b>
    <b>**Redis star topology** — LINDA channel (messages), LOGS channel (monitoring), BB SET (blackboard)</b>
    <b>**Single-file multi-agent** — :- agent(name). sets context for subsequent rules</b>
    <b>**Full DALI feature set** — reactive rules, internal events, goals, constraints, tell/told</b>
    <b>**Integrated web UI** — real-time dashboard, event sender, belief inspector</b>
    <b>**AI Oracle** — connect to any LLM via OpenRouter (GPT, Claude, Gemini, etc.)</b>
    <b>**LAN-ready** — distributed deployment across multiple machines</b>
    <b>**Docker support** — single command setup with Redis included</b>
  </bullets>
</slide>

<slide type="content">
  <title>Prerequisites</title>
  <text>Two ways to run DALI2:</text>
  <bullets>
    <b>**With Docker (recommended)** — only Docker Desktop needed; Redis starts automatically</b>
    <b>**Without Docker** — requires SWI-Prolog (>= 9.0) and Redis (>= 6.0)</b>
    <b>**Redis** — must be running before starting DALI2; default: localhost:6379</b>
    <b>**Environment variables** — REDIS_HOST and REDIS_PORT to override defaults</b>
    <b>**Optional** — OPENROUTER_API_KEY for AI Oracle integration</b>
  </bullets>
</slide>

<slide type="code">
  <title>Quick Start</title>
  <text>Start DALI2 with Docker or SWI-Prolog, then open http://localhost:8080</text>
  <code size="14">%% With Docker (recommended) — Redis starts automatically
docker compose up --build

%% Choose a different agent file
AGENT_FILE=examples/emergency.pl docker compose up --build

%% Without Docker — start Redis first, then:
redis-server
swipl -l src/server.pl -g main -- 8080 examples/agriculture.pl

%% Windows
run.bat</code>
</slide>

<!-- ═══════════════════════════════════════════════════════════
     PART 2: ARCHITECTURE (Slides 8-14)
     ═══════════════════════════════════════════════════════════ -->

<slide type="section">
  <title>Architecture</title>
  <subtitle>Redis Star Topology · Process-per-Agent · Deployment Modes</subtitle>
</slide>

<slide type="diagram">
  <title>Redis Star Topology</title>
  <text>All agents communicate through a central Redis instance. No direct agent-to-agent connections.</text>
  <diagram>
                    +---------------+
                    |    Redis      |
                    |  +---------+  |
                    |  | LINDA   |  |  &lt;-- pub/sub for messages
                    |  | LOGS    |  |  &lt;-- pub/sub for monitoring
                    |  |  BB     |  |  &lt;-- SET for shared blackboard
                    |  +---------+  |
                    +-------+-------+
              +-------------+-------------+
              |             |             |
        +-----+-----+ +----+------+ +----+------+
        | Agent 1   | | Agent 2   | | Agent N   |
        | (swipl)   | | (swipl)   | | (swipl)   |
        +-----------+ +-----------+ +-----------+

        +----------------------------------------+
        |       Master Server (:8080)            |
        |  Web UI - REST API - Cluster View      |
        +----------------------------------------+</diagram>
</slide>

<slide type="content">
  <title>Three Communication Channels</title>
  <text>Redis provides three distinct channels for different purposes:</text>
  <bullets>
    <b>**LINDA channel** — pub/sub for inter-agent messages. Format: TO:CONTENT:FROM. All agents subscribe. TO=* for broadcast.</b>
    <b>**LOGS channel** — pub/sub for monitoring. Agents publish log entries; external tools can subscribe.</b>
    <b>**BB (Blackboard)** — Redis SET for shared data. Replaces DALI's Linda tuple space. Agents use bb_read/bb_write/bb_remove.</b>
  </bullets>
</slide>

<slide type="content">
  <title>Process-per-Agent Model</title>
  <text>Each agent runs as a separate OS process — true parallelism and isolation.</text>
  <bullets>
    <b>**One swipl process per agent** — the master server spawns each agent as an independent process</b>
    <b>**Cycle-based execution** — each agent has its own event loop with configurable cycle duration</b>
    <b>**Independent failure** — if one agent crashes, others continue running</b>
    <b>**Redis decoupling** — agents don't know each other's location; all routing is via Redis</b>
    <b>**Scalable** — add agents by adding :- agent(name). declarations to the .pl file</b>
    <b>**No shared memory** — all inter-agent state goes through Redis (messages or blackboard)</b>
  </bullets>
</slide>

<slide type="content">
  <title>Deployment Modes</title>
  <text>Same code works in all three modes — just change how processes are started.</text>
  <bullets>
    <b>**Mode A: All-in-one** — one Redis, one server, all agents in one terminal. Simplest setup.</b>
    <b>**Mode B: Multi-terminal** — multiple server instances on the same machine share one Redis. Each starts a subset of agents with --agents flag.</b>
    <b>**Mode C: Multi-machine** — same as Mode B but across LAN. All nodes point REDIS_HOST to the Redis machine.</b>
    <b>**No code changes** — agents communicate through Redis regardless of deployment mode</b>
    <b>**Cluster panel** — web UI shows all agents across all nodes</b>
  </bullets>
</slide>

<slide type="diagram">
  <title>Distributed Deployment (Mode C)</title>
  <text>Multiple machines on the same LAN, all pointing to one Redis instance.</text>
  <diagram>
  +--------------------+
  | Machine X          |
  | (192.168.1.10)     |
  |  +------------+    |
  |  |   Redis    |    |
  |  +------+-----+    |
  +---------+-----------+
            |
     +------+------+
     |             |
+----+-------+ +---+--------+
| Machine Y  | | Machine Z  |
| port 8081  | | port 8082  |
| soil_sensor| | crop_advisor|
| weather_mon| | irrigation |
| logger     | | farmer     |
+------------+ +------------+</diagram>
</slide>

<slide type="code">
  <title>Deployment Commands</title>
  <text>Examples for each deployment mode:</text>
  <code size="12">%% Mode A: All-in-one (simplest)
docker compose up --build

%% Mode B: Multi-terminal (same machine, shared Redis)
%% Terminal 1 — sensors node (port 8081)
swipl -l src/server.pl -g main -- 8081 examples/agriculture.pl \
  --name sensors --agents soil_sensor,weather_monitor,logger

%% Terminal 2 — advisors node (port 8082)
swipl -l src/server.pl -g main -- 8082 examples/agriculture.pl \
  --name advisors --agents crop_advisor,irrigation_controller,farmer_agent

%% Mode C: Multi-machine — set REDIS_HOST
REDIS_HOST=192.168.1.10 swipl -l src/server.pl -g main -- 8081 \
  examples/agriculture.pl --name sensors --agents soil_sensor,logger</code>
</slide>

<!-- ═══════════════════════════════════════════════════════════
     PART 3: LANGUAGE FUNDAMENTALS (Slides 15-20)
     ═══════════════════════════════════════════════════════════ -->

<slide type="section">
  <title>Language Fundamentals</title>
  <subtitle>Agent Declarations · Suffixes · Operators · Beliefs</subtitle>
</slide>

<slide type="code">
  <title>Agent Declaration</title>
  <text>Every agent is declared with :- agent(Name, Options). All subsequent rules belong to that agent.</text>
  <code size="14">%% Agent with options
:- agent(my_agent, [cycle(1)]).

%% Agent without options (defaults to cycle(1))
:- agent(simple_agent).

%% Multiple agents in one file
:- agent(sensor, [cycle(1)]).
%% ... sensor rules here ...

:- agent(coordinator, [cycle(2)]).
%% ... coordinator rules here ...

%% Options:
%%   cycle(N)  — cycle duration in seconds (default: 1)</code>
</slide>

<slide type="table">
  <title>Suffixes — Event and Predicate Types</title>
  <text>DALI2 uses single-letter suffixes to classify predicates. Identical to original DALI.</text>
  <table>
    <header><cell>Suffix</cell><cell>Meaning</cell><cell>Example</cell><cell>Description</cell></header>
    <row><cell>E</cell><cell>External Event</cell><cell>alarmE(Type, Loc)</cell><cell>React to incoming messages or injected events</cell></row>
    <row><cell>I</cell><cell>Internal Event</cell><cell>check_statusI</cell><cell>Proactive event that fires automatically</cell></row>
    <row><cell>A</cell><cell>Action</cell><cell>dispatchA(Type, Loc)</cell><cell>Named, reusable action definition</cell></row>
    <row><cell>N</cell><cell>Present Event</cell><cell>obstacle_aheadN</cell><cell>Atomic environment observation (use in body only)</cell></row>
    <row><cell>P</cell><cell>Past Check</cell><cell>alarmP(Type)</cell><cell>Check if event exists in past memory</cell></row>
  </table>
</slide>

<slide type="table">
  <title>Operators — Rule Types</title>
  <text>DALI2 operators define different kinds of rules. All identical to original DALI.</text>
  <table>
    <header><cell>Operator</cell><cell>Name</cell><cell>Syntax</cell><cell>Description</cell></header>
    <row><cell>:&gt;</cell><cell>Reactive Rule</cell><cell>eventE(X) :&gt; body.</cell><cell>React to external or internal events</cell></row>
    <row><cell>:&lt;</cell><cell>Condition-Action</cell><cell>cond :&lt; action.</cell><cell>Edge-triggered: fires once when cond becomes true</cell></row>
    <row><cell>~/</cell><cell>Export Past</cell><cell>action ~/ past1, past2.</cell><cell>Fire when past events match, then consume them</cell></row>
    <row><cell>&lt;/</cell><cell>Export Past (NOT done)</cell><cell>action &lt;/ past1.</cell><cell>Fire only if action was NOT done</cell></row>
    <row><cell>?/</cell><cell>Export Past (done)</cell><cell>action ?/ past1.</cell><cell>Fire only if action WAS done</cell></row>
    <row><cell>:~</cell><cell>Constraint</cell><cell>:~ condition.</cell><cell>Invariant checked every cycle</cell></row>
  </table>
</slide>

<slide type="code">
  <title>Beliefs</title>
  <text>Beliefs are facts the agent holds true. They can be declared statically and modified at runtime.</text>
  <code size="14">:- agent(thermostat, [cycle(2)]).

%% Static beliefs (declared in agent file)
believes(target_temp(22)).
believes(current_temp(20)).
believes(mode(idle)).

%% Runtime operations (used in rule bodies):
%%   assert_belief(Fact)    — add a belief
%%   retract_belief(Fact)   — remove a belief
%%   believes(Fact)         — check if a belief exists (ontology-aware)

%% Example in a rule body:
update_tempE(NewT) :>
    retract_belief(current_temp(_)),
    assert_belief(current_temp(NewT)),
    log("Temperature updated to ~w", [NewT]).</code>
</slide>

<!-- ═══════════════════════════════════════════════════════════
     PART 4: REACTIVE RULES (Slides 21-25)
     ═══════════════════════════════════════════════════════════ -->

<slide type="section">
  <title>Reactive Rules</title>
  <subtitle>External Events · Actions · Communication · Logging</subtitle>
</slide>

<slide type="code">
  <title>External Events (E + :>)</title>
  <text>React to incoming messages from other agents or injected events. The E suffix is stripped when matching. (From original DALI slides: "Events and Actions")</text>
  <code size="14">:- agent(doorman, [cycle(1)]).

%% React to a bell ring event (DALI slide p.7)
bell_ringsE :> do(open_door).

:- agent(access_controller, [cycle(1)]).

%% Access controller (DALI slide p.9)
situation_okE :> do(open_access).
situation_unsafeE :> do(limit_access).

:- agent(security, [cycle(1)]).

%% React to alarm with pattern matching (DALI slide p.8)
alarmE(Kind) :>
    log("Alarm received: ~w", [Kind]),
    do(alert_operator).</code>
</slide>

<slide type="code">
  <title>Communication: send and messageA</title>
  <text>Two equivalent ways to send messages — DALI2 style (send) and classic DALI style (messageA). (DALI slides p.7, p.28)</text>
  <code size="14">:- agent(agent1, [cycle(1)]).

%% Classic DALI style (from DALI basic example)
goE :> messageA(agent2, send_message(go, Me)).

%% DALI2 style — concise, identical behavior
goE :> send(agent2, go).

%% messageA(To, Comm_primitive(Content, From))
%% is equivalent to send(To, Content)

%% Other communication predicates:
%%   send(Agent, Content)     — send to one agent
%%   broadcast(Content)       — send to all agents
%%   from(Sender)             — get sender of current message
%%   reply_to(Content)        — reply to current message sender</code>
</slide>

<slide type="code">
  <title>Actions (A Suffix)</title>
  <text>Named, reusable actions. Actions are recorded in past memory as did(Action). (DALI slides p.7, p.9)</text>
  <code size="13">:- agent(doorman, [cycle(1)]).

%% Action definitions (DALI slide p.7)
open_doorA :- log("Opening the door").
get_keyA :- log("Getting the key first").

%% Actions with preconditions (DALI slide p.7)
open_doorA :&lt; believes(have_key(true)).
open_doorA :&lt; do(get_key).

%% External event triggers an action via do/1
bell_ringsE :&gt; do(open_door).

%% After do(open_door), has_past(did(open_door)) is true
%% After do(get_key), has_past(did(get_key)) is true
%% This enables export past (?/) and past checks (P suffix)</code>
</slide>

<slide type="code">
  <title>Logging</title>
  <text>Agents log messages to the LOGS Redis channel. Visible in the web UI event log.</text>
  <code size="14">:- agent(sensor, [cycle(1)]).

%% Simple log message
readingE(Value) :>
    log("Sensor reading received"),
    log("Value: ~w", [Value]).

%% Format syntax (same as Prolog format/2)
%%   ~w   — write term
%%   ~d   — write integer
%%   ~a   — write atom
%%   ~f   — write float

alarmE(Type, Loc) :>
    log("ALARM: type=~w location=~w", [Type, Loc]).</code>
</slide>

<!-- ═══════════════════════════════════════════════════════════
     PART 5: INTERNAL EVENTS & PERIODIC (Slides 26-31)
     ═══════════════════════════════════════════════════════════ -->

<slide type="section">
  <title>Internal Events and Periodic Tasks</title>
  <subtitle>Proactive Behavior · Timers · Condition Monitors</subtitle>
</slide>

<slide type="code">
  <title>Internal Events (I + :>)</title>
  <text>Internal conclusions initiate further inference (proactivity!). Defined by handler + configuration. (DALI slides p.11–12)</text>
  <code size="13">:- agent(cook, [cycle(2)]).

%% Internal event: cake is ready (DALI slide p.11)
helper(ready(cake)) :- has_past(put_in_oven(cake)).
readyI(cake) :>
    do(take_from_oven(cake)),
    do(switch_off_oven),
    log("Cake is ready! Bon appetit.").
internal_event(ready(cake), 5, forever, true, forever).

:- agent(security, [cycle(1)]).

%% Attack detection (DALI slide p.12)
helper(attack_happening(Cond)) :- believes(suspicious(Cond)).
attack_happeningI(Cond) :>
    do(start_emergency(Cond)), do(alert_operator).
internal_event(attack_happening(_), 3, forever, true, forever).</code>
</slide>

<slide type="table">
  <title>internal_event/5 — Configuration</title>
  <text>Each parameter controls a different aspect of the internal event's lifecycle.</text>
  <table>
    <header><cell>Argument</cell><cell>Position</cell><cell>Values</cell><cell>Description</cell></header>
    <row><cell>Event</cell><cell>1</cell><cell>atom</cell><cell>Event name (without I suffix)</cell></row>
    <row><cell>Period</cell><cell>2</cell><cell>0, N (seconds)</cell><cell>Minimum interval between firings</cell></row>
    <row><cell>Repetition</cell><cell>3</cell><cell>forever, times(N), change([...])</cell><cell>How many times / when to reset counter</cell></row>
    <row><cell>StartCond</cell><cell>4</cell><cell>true, believes(...), has_past(...)</cell><cell>Guard: must be true before event fires</cell></row>
    <row><cell>StopCond</cell><cell>5</cell><cell>forever, believes(...), has_past(...)</cell><cell>Stop condition: event stops when true</cell></row>
  </table>
</slide>

<slide type="code">
  <title>Internal Event Options</title>
  <text>Advanced options for fine-grained control over internal event behavior.</text>
  <code size="12">:- agent(thermostat, [cycle(2)]).

%% Fire at most 3 times, reset when current_temp changes
startup_diagnosticI :>
    log("Startup diagnostic").
internal_event(startup_diagnostic, 0, change([current_temp(_)]), true, forever).

%% Fire only when mode is cooling (trigger/start condition)
cooling_monitorI :>
    believes(current_temp(T)),
    log("Monitoring cooling, temp: ~w", [T]).
internal_event(cooling_monitor, 0, forever, believes(mode(cooling)), forever).

%% Fire only during work hours
work_hours_checkI :>
    log("Work hours system check").
internal_event(work_hours_check, 10, forever, true, in_date(time(9,0), time(17,0))).</code>
</slide>

<slide type="code">
  <title>Periodic Tasks (every)</title>
  <text>Simple periodic execution at fixed intervals. No conditional options — just fires every N seconds. [NEW in DALI2]</text>
  <code size="14">:- agent(sensor, [cycle(1)]).

%% Simple periodic log (one-liner)
every(10, log("Heartbeat")).

%% Periodic with body
every(30) :-
    log("Checking system health"),
    send(dashboard, health_check).

%% Difference from internal events:
%%   every     — simple timer, no conditions
%%   internal  — full lifecycle (start/stop/change/trigger conditions)</code>
</slide>

<slide type="code">
  <title>Condition Monitors (when)</title>
  <text>Check a condition every cycle. Level-triggered: fires every cycle while condition holds. [NEW in DALI2]</text>
  <code size="14">:- agent(thermostat, [cycle(2)]).

%% Fire every cycle while temperature is above 30
when(believes(temperature(T)), T > 30) :-
    send(ac_controller, cool_down).

:- agent(alarm, [cycle(1)]).

%% Check a past event condition
when(has_past(intrusion_detected)) :-
    send(security, alert).

%% Supports: believes(...), has_past(...), learned(...),
%%           bb_read(...), arithmetic comparisons</code>
</slide>

<!-- ═══════════════════════════════════════════════════════════
     PART 6: CONDITION & ENVIRONMENT RULES (Slides 32-37)
     ═══════════════════════════════════════════════════════════ -->

<slide type="section">
  <title>Condition and Environment Rules</title>
  <subtitle>Condition-Action · Present Events · Multi-Events · Constraints</subtitle>
</slide>

<slide type="code">
  <title>Condition-Action Rules (:&lt;)</title>
  <text>Edge-triggered: fires exactly once when condition transitions from false to true. (DALI slides p.7, p.9: actions with preconditions)</text>
  <code size="13">:- agent(access_controller, [cycle(1)]).

%% Access controller (DALI slide p.9)
%% open_access only if access is currently limited
has_past(did(limit_access)) :&lt; do(open_access).

%% limit_access only if access is currently open
has_past(did(open_access)) :&lt; do(limit_access).

:- agent(doorman, [cycle(1)]).

%% Actions with preconditions (DALI slide p.7)
%% open_door only when we have the key
believes(have_key(true)) :&lt; do(open_door).

%% Edge-triggered: fires ONCE when condition becomes true,
%% then waits for it to become false before firing again.</code>
</slide>

<slide type="content">
  <title>when vs :&lt; — Level vs Edge Triggered</title>
  <text>Both check conditions every cycle, but they fire differently:</text>
  <bullets>
    <b>**when (level-triggered)** — fires EVERY CYCLE while the condition is true. Good for continuous monitoring.</b>
    <b>**:&lt; (edge-triggered)** — fires ONCE when the condition becomes true. Waits for false-to-true transition before firing again.</b>
    <b>**Example:** temperature goes above 30 and stays there for 10 cycles</b>
    <b>**when** — fires 10 times (once per cycle)</b>
    <b>**:&lt;** — fires 1 time (on the first cycle where T > 30)</b>
    <b>**Use when** for continuous reactions (keep sending alerts while hot)</b>
    <b>**Use :&lt;** for one-shot reactions (notify once when it becomes hot)</b>
  </bullets>
</slide>

<slide type="code">
  <title>Present Events (N Suffix)</title>
  <text>Atomic environmental observations — the agent perceives them but does not define them. Three stages of an event in DALI (slides p.6–7): N (present) → E (reaction) → P (past memory).</text>
  <code size="13">%% DALI slide p.6-7: "Perspectives on External Events"
%% alarm_signalN = present event (Stage 1: perceived, no reaction yet)
%% alarm_signalE = external event (Stage 2: reaction triggered)
%% alarm_signalP = past event   (Stage 3: remembered)

%% Original DALI example (slide p.7):
%%   visitor_arrived :- bell_ringsN.  (reasoning about what is happening)
%%   bell_ringsE :&gt; open_doorA.       (reacting to the event)

:- agent(doorman, [cycle(1)]).

%% In DALI2, present events are atomic observations.
%% Use believes() or has_past() to reason about them:
bell_ringsE :&gt; do(open_door), log("Visitor arrived!").

%% WRONG: bell_ringsN :- do(something). &lt;-- NOT allowed!
%% Present events are atomic, they have no :- definition.</code>
</slide>

<slide type="code">
  <title>Multi-Events with Delta-T</title>
  <text>Conjunction of events in the head of a reactive rule. Must occur within a simultaneity interval (delta-t). (DALI slides p.8, Double Events example)</text>
  <code size="13">:- agent(security, [cycle(1)]).

%% DALI slide p.8: conjunction of alarms
alarmE(K1), alarmE(K2), within(60) :&gt;
    log("Multiple alarms: ~w and ~w", [K1, K2]),
    do(close_access), do(alert_operator).

:- agent(fire_controller, [cycle(1)]).

%% DALI Double Events example: fire detection
heat_sensedE(Zone, Temp), smoke_detectedE(Zone, Level), within(60) :&gt;
    log("Fire in zone ~w! temp=~w smoke=~w", [Zone, Temp, Level]),
    do(fire_response(Zone, Temp)).

%% within(N) = DALI's t60. / deltat(N) directive
%% Without within(), events match regardless of timing.</code>
</slide>

<slide type="code">
  <title>Constraints (:~)</title>
  <text>Invariant conditions checked every cycle. If violated (condition is false), the handler fires. Syntax: Cond :~ Handler.</text>
  <code size="14">:- agent(thermostat, [cycle(2)]).

%% Safety constraint: temperature must stay below 50
%% Left = condition that SHOULD hold; Right = handler if violated
(believes(current_temp(T)), T &lt; 50) :~ (
    log("CONSTRAINT VIOLATED: Temp ~w exceeds limit!", [T]),
    send(coordinator, emergency(overheating, T))
).

%% Simple constraint (log-only handler)
believes(config_loaded) :~ log("Config not loaded!").

%% Fires when condition is FALSE (violated).
%% Checked automatically every agent cycle.</code>
</slide>

<!-- ═══════════════════════════════════════════════════════════
     PART 7: GOALS & MEMORY (Slides 38-45)
     ═══════════════════════════════════════════════════════════ -->

<slide type="section">
  <title>Goals and Memory</title>
  <subtitle>Achieve Goals · Test Goals · Residue · Past Events · Export Past</subtitle>
</slide>

<slide type="code">
  <title>Achieve Goals (obt_goal)</title>
  <text>Goals are automatically attempted until they succeed, then expire. (DALI slides p.14–15: "each goal pG will be automatically attempted until it succeeds")</text>
  <code size="14">:- agent(patient, [cycle(2)]).

%% Goal: find a good doctor (inspired by DALI slides p.38-40)
obt_goal(believes(chosen_doctor(_))) :-
    log("Searching for a good doctor..."),
    send(friend_nurse, query_ref(ability(_, _))).

:- agent(security, [cycle(1)]).

%% Goal: keep trying to connect (DALI slide p.17)
obt_goal(believes(connection_established)) :-
    log("Attempting connection..."),
    send(server, connect_request).

%% Lifecycle: try each cycle until condition met,
%% then mark as "achieved" and stop.</code>
</slide>

<slide type="code">
  <title>Test Goals (test_goal)</title>
  <text>Try the plan once, then check if the goal succeeded. Does not retry.</text>
  <code size="14">:- agent(coordinator, [cycle(2)]).

%% Test if we received at least one alert
test_goal(believes(alerts_received(N)), N > 0) :-
    log("Testing if any alerts received...").

%% Lifecycle:
%%   1. Execute plan ONCE
%%   2. Check goal condition
%%   3. Record result: succeeded / failed / error
%%   4. Will NOT retry (unlike obt_goal)
%%
%% Difference from obt_goal:
%%   obt_goal — persistent, retries until achieved
%%   test_goal — one-shot, records result</code>
</slide>

<slide type="code">
  <title>Residue Goals</title>
  <text>When achieve(Goal) is called but the goal is not yet satisfiable, it becomes a residue goal — automatically retried each cycle.</text>
  <code size="14">:- agent(coordinator, [cycle(2)]).

%% The achieve call queues the goal as residue
start_residue_testE :>
    log("Starting residue goal test..."),
    achieve(has_past(residue_resolved)).

%% Lifecycle:
%%   1. achieve(Goal) called — Goal not yet true
%%   2. Goal queued as RESIDUE
%%   3. Each cycle: re-check all residue goals
%%   4. When condition becomes true -> achieved, removed from residue
%%
%% DALI equivalent: tenta_residuo(goal)</code>
</slide>

<slide type="code">
  <title>Past Event Lifetime</title>
  <text>Control how long past events are kept in memory. Mirrors DALI's directives (slides p.9, p.17): "keep open_accessP until limit_accessA".</text>
  <code size="14">:- agent(access_controller, [cycle(1)]).

%% DALI slide p.9: past events with terminating conditions
%% "keep open_accessP until close_accessA"
past_event(did(open_access), 3600).   %% keep for 1 hour
past_event(did(limit_access), 3600).

%% DALI slide p.17: keep bornP(daniele,...) forever
past_event(born(_, _), forever).

%% Check past in rule bodies:
%%   has_past(Event)         — is event in past memory?
%%   has_past(Event, Time)   — with timestamp
%%   evp(Event)              — DALI-style past check
%%   eventP(Args)            — DALI-style with P suffix</code>
</slide>

<slide type="code">
  <title>Remember Events</title>
  <text>Expired past events move to a "remember" tier. Control remember lifetime and limits.</text>
  <code size="14">:- agent(sensor, [cycle(2)]).

%% Past expires after 30s, then remembered for 5 minutes
past_event(sensor_data(_), 30).
remember_event(sensor_data(_), 300).

%% Keep only the last 10 remembered events
remember_event_mod(sensor_data(_), number(10), last).
%%   number(N) — max entries
%%   last      — keep newest  |  first — keep oldest

%% Check remembered events:
%%   has_remember(Event)
%%   has_remember(Event, Time)</code>
</slide>

<slide type="code">
  <title>Export Past — ~/ Operator</title>
  <text>Fire when past event patterns match, then CONSUME (delete) them. Enables one-shot reactions to event combinations.</text>
  <code size="14">:- agent(fire_controller, [cycle(1)]).

%% When both heat and smoke detected, consume and respond
%% (inspired by DALI Double Events fire detection example)
send(dispatch, fire_response(Zone)) ~/
    heat_sensed(Zone, _), smoke_detected(Zone, _).

:- agent(security, [cycle(1)]).

%% When alarm and breach both in past, consume and escalate
do(full_lockdown) ~/
    alarm(_, _), breach_detected(_).

%% After firing: matched past events are REMOVED.
%% Rule can fire again only with NEW events.</code>
</slide>

<slide type="code">
  <title>Export Past — &lt;/ and ?/ Operators</title>
  <text>Conditional export past based on whether an action was or was not done. (DALI slide p.9: access controller pattern)</text>
  <code size="14">:- agent(fire_controller, [cycle(1)]).

%% &lt;/ — Fire only if response was NOT done yet
%% (fire detected but no response action taken)
log("WARNING: fire detected but no response!") &lt;/
    heat_sensed(_, _), smoke_detected(_, _).

%% ?/ — Fire only if response WAS already done
%% (follow-up after fire_response action completed)
send(logger, log_event(followup_check)) ?/
    heat_sensed(Zone, _), smoke_detected(Zone, _).

%% Summary:
%%   ~/  — fire when past events match (unconditional)
%%   &lt;/  — fire when past events match AND action NOT done
%%   ?/  — fire when past events match AND action WAS done</code>
</slide>

<!-- ═══════════════════════════════════════════════════════════
     PART 8: COMMUNICATION & FILTERING (Slides 46-52)
     ═══════════════════════════════════════════════════════════ -->

<slide type="section">
  <title>Inter-Agent Communication</title>
  <subtitle>Tell/Told Filtering · Priority Queues · FIPA Messages · Proposals</subtitle>
</slide>

<slide type="code">
  <title>Tell/Told Filtering (with Body Conditions)</title>
  <text>Filter incoming/outgoing messages with real conditions. (DALI slides p.32–33, p.39–41: trust-based filtering)</text>
  <code size="12">:- agent(patient, [cycle(2)]).

%% TOLD — receive filter (DALI slides p.33, p.39)
%% Only accept messages from agents we trust
told(Sender, send_message(_)) :-
    believes(trust(patient, Sender, Trust)), Trust &gt; 0.3.
told(Sender, execute_proc(Proc)) :-
    \+ has_past(unreliable(Sender)).
told(_, inform(_, _), 70) :- true.       %% always accept inform
told(_, emergency(_)).                   %% 2-arg form (priority=0)

%% TELL — send filter (DALI slide p.37)
tell(To, _, send_message(_)) :-
    believes(trust(patient, To, Trust)), Trust &gt; 0.3.
tell(_, _, inform(_, _)) :- true.</code>
</slide>

<slide type="content">
  <title>Priority Queue</title>
  <text>When an agent has told rules with numeric priorities, incoming messages are sorted before processing.</text>
  <bullets>
    <b>**Priority values** — the third argument of told/3 is a numeric priority</b>
    <b>**Highest first** — messages with higher priority are processed first in each cycle</b>
    <b>**Example:** emergency(200) is processed before sensor_data(30)</b>
    <b>**Multiple messages per cycle** — all pending messages are sorted, then processed in order</b>
    <b>**Rejected messages** — messages not matching any told pattern are rejected with a log entry</b>
    <b>**AI Oracle filtering** — tell/told rules also apply to AI queries and responses</b>
  </bullets>
</slide>

<slide type="table">
  <title>FIPA Message Types</title>
  <text>DALI2 supports FIPA-ACL message types for structured inter-agent communication.</text>
  <table>
    <header><cell>Type</cell><cell>Syntax</cell><cell>Receiver Semantics</cell></header>
    <row><cell>inform</cell><cell>send(To, inform(Content))</cell><cell>Normal handler + past record</cell></row>
    <row><cell>confirm</cell><cell>send(To, confirm(Fact))</cell><cell>Records confirmed(Fact) in past</cell></row>
    <row><cell>disconfirm</cell><cell>send(To, disconfirm(Fact))</cell><cell>Removes confirmed(Fact) from past</cell></row>
    <row><cell>propose</cell><cell>send(To, propose(Action))</cell><cell>Fires on_proposal handler</cell></row>
    <row><cell>accept_proposal</cell><cell>send(To, accept_proposal(A))</cell><cell>Normal handler</cell></row>
    <row><cell>reject_proposal</cell><cell>send(To, reject_proposal(A))</cell><cell>Normal handler</cell></row>
    <row><cell>query_ref</cell><cell>send(To, query_ref(Query))</cell><cell>Auto-responds with matching beliefs</cell></row>
    <row><cell>agree / refuse</cell><cell>send(To, agree(C))</cell><cell>Normal handler</cell></row>
    <row><cell>failure / cancel</cell><cell>send(To, failure(A, R))</cell><cell>Normal handler</cell></row>
  </table>
</slide>

<slide type="code">
  <title>FIPA Examples</title>
  <text>FIPA primitives for influencing "mental state" of other agents. (DALI slides p.30: propose, accept_proposal, send_message)</text>
  <code size="13">:- agent(patient, [cycle(2)]).

%% Ask a friend about a doctor (DALI slides p.38-39)
get_adviceE(Doctor) :>
    send(friend_nurse, query_ref(ability(Doctor, _))).

%% Handle the response
informE(query_ref(ability(Doc, _)), values(V)) :>
    log("Advice about ~w: ~w", [Doc, V]).

:- agent(friend_nurse, [cycle(1)]).

believes(ability(lung_doctor1, 0.7)).
believes(ability(lung_doctor2, 0.3)).

%% query_ref auto-responds with matching beliefs
%% confirm/disconfirm for fact management
%% propose/accept_proposal for negotiation</code>
</slide>

<slide type="code">
  <title>Action Proposals (on_proposal)</title>
  <text>Negotiation via FIPA propose/accept/reject. (DALI slides p.30: propose(Action,[cond1,...,condn], A2))</text>
  <code size="12">:- agent(lung_doctor1, [cycle(2)]).

%% Handle incoming proposals (inspired by DALI slide p.30)
on_proposal(treat(Patient)) :-
    believes(available(true)),
    from(Sender),
    log("Accepting treatment for ~w", [Patient]),
    accept_proposal(Sender, treat(Patient)).

on_proposal(treat(_)) :-
    from(Sender),
    reject_proposal(Sender, treat(_)).

:- agent(patient, [cycle(2)]).

%% Send a proposal to a doctor
choose_doctorE(Doctor) :>
    send(Doctor, propose(treat(patient))).</code>
</slide>

<!-- ═══════════════════════════════════════════════════════════
     PART 9: ADVANCED FEATURES (Slides 53-60)
     ═══════════════════════════════════════════════════════════ -->

<slide type="section">
  <title>Advanced Features</title>
  <subtitle>Ontology · Learning · Helpers · Blackboard · AI Oracle</subtitle>
</slide>

<slide type="code">
  <title>Ontology Declarations</title>
  <text>Meta-reasoning on messages exploiting ontologies. (DALI slides p.34–35: "piove" ↔ "it_rains", symmetric relations like loves(a,b) ↔ loves(b,a))</text>
  <code size="13">:- agent(patient, [cycle(2)]).

%% DALI slide p.35: ontology for understanding messages
%% "piove" and "it_rains" are equivalent
ontology(same_as(piove, it_rains)).
ontology(same_as(malato, ill)).
ontology(eq_property(temperature, temp)).

%% Symmetric relations (DALI slide p.35)
%% loves(pippo, pina) matches loves(pina, pippo)
ontology(symmetric(loves)).
ontology(symmetric(near)).

%% External ontology file
ontology_file('local_ontology.pl').

%% If told fails, meta-rules automatically applied
%% on message contents (DALI slide p.34)</code>
</slide>

<slide type="code">
  <title>Learning Rules (learn_from)</title>
  <text>Agents learn from experience. Inspired by DALI slides p.40: patient learns which doctors are effective. [NEW in DALI2]</text>
  <code size="13">:- agent(patient, [cycle(2)]).

%% Learn doctor effectiveness from treatment outcomes
%% (inspired by DALI slides p.40: recovered_by / not_recovered_by)
learn_from(treatment(Doctor, recovered), good_doctor(Doctor)).
learn_from(treatment(Doctor, not_recovered), bad_doctor(Doctor)).

%% Use learned knowledge to choose next doctor
need_doctorE :>
    ( learned(treatment(Doc, _), good_doctor(Doc)) ->
        log("Choosing previously effective doctor: ~w", [Doc]),
        send(Doc, request_appointment)
    ;
        log("No learned preference, asking friends..."),
        send(friend_nurse, query_ref(ability(_, _)))
    ).

%% Predicates: learn(P,O), learned(P,O), forget(P)</code>
</slide>

<slide type="code">
  <title>Helpers</title>
  <text>Utility predicates callable from rule bodies. In original DALI, plain clauses served this role (e.g. access_is_limited :- limit_accessP). [NEW in DALI2]</text>
  <code size="13">:- agent(access_controller, [cycle(1)]).

%% DALI slide p.9: helper conditions for access state
helper(access_is_limited) :- has_past(did(limit_access)).
helper(access_is_open) :- has_past(did(open_access)).

%% Used in condition-action rules:
has_past(did(limit_access)) :&lt; do(open_access).
has_past(did(open_access)) :&lt; do(limit_access).

:- agent(cook, [cycle(2)]).

%% DALI slide p.11: helper to check if cake is ready
helper(ready(cake)) :- has_past(put_in_oven(cake)).

%% Helpers can use: believes, has_past, log, send, etc.</code>
</slide>

<slide type="code">
  <title>Blackboard Operations</title>
  <text>Shared blackboard via Redis SET. All agents can read/write tuples. [NEW in DALI2]</text>
  <code size="14">:- agent(sensor, [cycle(1)]).

%% Write to shared blackboard
readingE(Value) :>
    bb_write(env_data(temperature, Value)),
    log("Wrote temperature to blackboard").

:- agent(monitor, [cycle(2)]).

%% Read from blackboard (non-destructive)
when(bb_read(env_data(temperature, T)), T > 40) :-
    log("Blackboard says temperature is ~w!", [T]).

%% Remove from blackboard
clearE :>
    bb_remove(env_data(temperature, _)).</code>
</slide>

<slide type="two_column">
  <title>AI Oracle</title>
  <text>Connect to any LLM via OpenRouter. Agents send context, receive Prolog facts back.</text>
  <left>
    <b>**Configuration:** set OPENROUTER_API_KEY environment variable, web UI, or POST /api/ai/key</b>
    <b>**Optional:** if no key, ai_available fails and ask_ai returns suggestion(no_ai_available)</b>
    <b>**Models:** free (Gemini, Llama, Qwen, DeepSeek) and paid (GPT-4o, Claude, Gemini Pro)</b>
    <b>**Filtered:** tell/told rules apply to AI queries and responses</b>
  </left>
  <right type="code">
    <code>:- agent(advisor, [cycle(2)]).

analyzeE(Data) :>
    ( ai_available ->
        ask_ai(analyze(Data), Advice),
        log("AI: ~w", [Advice]),
        send(coord, ai_rec(Advice))
    ;
        log("AI not available")
    ).

%% ask_ai(Context, Result)
%% ask_ai(Context, Prompt, Result)</code>
  </right>
</slide>

<!-- ═══════════════════════════════════════════════════════════
     PART 10: AGENT LIFECYCLE (Slides 61-63)
     ═══════════════════════════════════════════════════════════ -->

<slide type="section">
  <title>Agent Lifecycle</title>
  <subtitle>Cycle-Based Event Loop · Past Memory</subtitle>
</slide>

<slide type="diagram">
  <title>Agent Cycle — 13 Steps</title>
  <text>Each agent runs a cycle-based event loop. All 13 steps execute in order every cycle.</text>
  <diagram>
+-------------------------------------------+
|            Agent Cycle                     |
+-------------------------------------------+
|  1. Process messages (priority queue)      |
|  2. Process injected events                |
|  3. Process internals (interval/change)    |
|  4. Process periodic tasks                 |
|  5. Process condition monitors             |
|  6. Process condition-actions              |
|  7. Process present events                 |
|  8. Process multi-events                   |
|  9. Process past reactions (export past)   |
| 10. Check constraints                      |
| 11. Process goals + residue goals          |
| 12. Clean up expired past events           |
| 13. Sleep for cycle duration               |
|     +--- repeat ---+                       |
+-------------------------------------------+</diagram>
</slide>

<slide type="content">
  <title>Past Memory Types</title>
  <text>Every event is recorded in past memory with a timestamp and source type.</text>
  <bullets>
    <b>**received(Content, From)** — external message received from another agent</b>
    <b>**injected(Event)** — event injected via REST API or web UI</b>
    <b>**internal(Event)** — internal event that fired</b>
    <b>**did(Action)** — action that was executed via do/1</b>
    <b>**goal_achieved(Goal)** — goal that was successfully achieved</b>
    <b>**confirmed(Fact)** — fact confirmed via FIPA confirm message</b>
    <b>Past events have configurable lifetimes (past_event/2)</b>
    <b>Expired past events move to the remember tier (remember_event/2)</b>
  </bullets>
</slide>

<!-- ═══════════════════════════════════════════════════════════
     PART 11: DSL REFERENCE (Slides 64-65)
     ═══════════════════════════════════════════════════════════ -->

<slide type="table">
  <title>DSL Predicates — Communication and Beliefs</title>
  <text>Predicates available inside rule bodies:</text>
  <table>
    <header><cell>Predicate</cell><cell>Description</cell></header>
    <row><cell>send(Agent, Content)</cell><cell>Send message to another agent (filtered by tell/told)</cell></row>
    <row><cell>broadcast(Content)</cell><cell>Send to all other agents</cell></row>
    <row><cell>from(Sender)</cell><cell>Get sender of current message</cell></row>
    <row><cell>reply_to(Content)</cell><cell>Reply to current message sender</cell></row>
    <row><cell>log(Format, Args)</cell><cell>Log a formatted message</cell></row>
    <row><cell>assert_belief(Fact)</cell><cell>Add a belief</cell></row>
    <row><cell>retract_belief(Fact)</cell><cell>Remove a belief</cell></row>
    <row><cell>believes(Fact)</cell><cell>Check if a belief exists (ontology-aware)</cell></row>
  </table>
</slide>

<slide type="table">
  <title>DSL Predicates — Past, Actions, Learning, Goals</title>
  <text>More predicates available inside rule bodies:</text>
  <table>
    <header><cell>Predicate</cell><cell>Description</cell></header>
    <row><cell>has_past(Event)</cell><cell>Check if event is in past memory</cell></row>
    <row><cell>has_remember(Event)</cell><cell>Check if event is in remember tier</cell></row>
    <row><cell>has_confirmed(Fact)</cell><cell>Check if fact was confirmed via FIPA</cell></row>
    <row><cell>do(Action)</cell><cell>Execute a named action (recorded as did(Action))</cell></row>
    <row><cell>helper(Goal)</cell><cell>Call a helper predicate</cell></row>
    <row><cell>learn(P, O) / learned(P, O)</cell><cell>Record / check learned associations</cell></row>
    <row><cell>achieve(Goal) / reset_goal(Goal)</cell><cell>Trigger / reset a goal</cell></row>
    <row><cell>bb_read(P) / bb_write(T) / bb_remove(P)</cell><cell>Blackboard operations (Redis)</cell></row>
    <row><cell>ask_ai(Context, Result)</cell><cell>Query AI Oracle (filtered by tell/told)</cell></row>
    <row><cell>onto_match(T1, T2)</cell><cell>Check ontology equivalence</cell></row>
  </table>
</slide>

<!-- ═══════════════════════════════════════════════════════════
     PART 12: EXAMPLES (Slides 66-71)
     ═══════════════════════════════════════════════════════════ -->

<slide type="section">
  <title>Examples</title>
  <subtitle>Smart Agriculture · Emergency Response · Feature Showcase · Distributed</subtitle>
</slide>

<slide type="two_column">
  <title>Example: Smart Agriculture</title>
  <text>File: examples/agriculture.pl — Precision agriculture with 6 agents.</text>
  <left>
    <b>**soil_sensor** — receives soil readings, validates via internal events</b>
    <b>**weather_monitor** — receives weather data, detects risks</b>
    <b>**crop_advisor** — analyzes reports (with AI), decides actions</b>
    <b>**irrigation_controller** — activates/reduces irrigation per field</b>
    <b>**farmer_agent** — receives advisories and status updates</b>
    <b>**logger** — central event logging</b>
  </left>
  <right type="code">
    <code>%% Test: send to soil_sensor
read_soil(25, 6.5, north_field)
%% Flow:
%%  sensor -> crop_advisor: soil_report
%%  advisor: low moisture -> irrigate
%%  advisor -> irrigation: irrigate(field)
%%  advisor -> farmer: advisory
%%  irrigation -> farmer: status</code>
  </right>
</slide>

<slide type="two_column">
  <title>Example: Emergency Response</title>
  <text>File: examples/emergency.pl — 9-agent emergency response system.</text>
  <left>
    <b>**sensor** — detects events, validates alarms (real vs false) via internal events</b>
    <b>**coordinator** — multi-step dispatch: AI analysis, waits for equipment, tracks completion</b>
    <b>**manager** — determines equipment (firetruck/bulldozer/respirator)</b>
    <b>**evacuator** — handles evacuation, reports back</b>
    <b>**responder** — responds with equipment, reports back</b>
    <b>**communicator** — notifies civilians (mary, john)</b>
    <b>**mary, john** — person agents receiving notifications</b>
    <b>**logger** — logs all events</b>
  </left>
  <right type="code">
    <code>%% Test: send to sensor
sense(fire, building_a)
%% Flow:
%%  sensor validates -> alarm
%%  coord -> evac + comm + mgr
%%  mgr: fire -> firetruck
%%  comm -> mary + john
%%  evac -> coord: evacuated
%%  coord: equip+loc -> responder
%%  responder -> coord: responded
%%  coord: RESOLVED</code>
  </right>
</slide>

<slide type="content">
  <title>Example: Feature Showcase</title>
  <text>File: examples/showcase.pl — Demonstrates ALL 32 DALI2 features in one file.</text>
  <bullets>
    <b>**thermostat** — internal events (interval, change, trigger, between), constraints, condition-action</b>
    <b>**sensor** — periodic tasks, learning, blackboard, past lifetime, goals</b>
    <b>**coordinator** — tell/told (body conditions), FIPA, multi-events (delta-t), goals, residue, export past, AI</b>
    <b>**logger** — ontology (inline + external file), helpers, condition monitors</b>
    <b>**worker** — action proposals (on_proposal), FIPA responses, actions, told rules (body conditions)</b>
    <b>**10-step test sequence** — from basic events to FIPA proposals to residue goals</b>
  </bullets>
</slide>

<slide type="two_column">
  <title>Example: Distributed Emergency</title>
  <text>Two separate DALI2 nodes communicating via shared Redis (star topology).</text>
  <left>
    <b>**Node 1: Sensors** (port 8081) — sensor + logger agents</b>
    <b>**Node 2: Responders** (port 8082) — coordinator + evacuator + responder + communicator</b>
    <b>Cross-node communication via Redis LINDA channel</b>
    <b>No code changes vs single-node</b>
    <b>Cluster panel shows all agents across nodes</b>
  </left>
  <right type="code">
    <code>%% Start with Docker Compose
docker compose \
  -f docker-compose.distributed.yml \
  up --build

%% Or manually (3 terminals):
%% T1: Redis
docker run -d -p 6379:6379 \
  redis:7-alpine
%% T2: Sensors (port 8080)
swipl -l src/server.pl -g main \
  -- 8080 emergency_sensors.pl
%% T3: Responders (port 8081)
swipl -l src/server.pl -g main \
  -- 8081 emergency_responders.pl</code>
  </right>
</slide>

<!-- ═══════════════════════════════════════════════════════════
     PART 13: COMPARISON & WEB UI (Slides 72-75)
     ═══════════════════════════════════════════════════════════ -->

<slide type="table">
  <title>DALI vs DALI2 — Key Differences</title>
  <text>DALI2 is fully backward compatible with DALI syntax. Key architectural differences:</text>
  <table>
    <header><cell>Aspect</cell><cell>DALI (SICStus)</cell><cell>DALI2 (SWI-Prolog)</cell></header>
    <row><cell>Source files</cell><cell>~20</cell><cell>8</cell></row>
    <row><cell>Agent definition</cell><cell>Multiple files</cell><cell>Single .pl file</cell></row>
    <row><cell>Communication</cell><cell>TCP sockets (Linda)</cell><cell>Redis star topology</cell></row>
    <row><cell>UI</cell><cell>Separate Python project</cell><cell>Integrated web UI</cell></row>
    <row><cell>AI integration</cell><cell>External Python TCP</cell><cell>Built-in (OpenRouter)</cell></row>
    <row><cell>Docker</cell><cell>Complex (SICStus)</cell><cell>Simple (swipl image)</cell></row>
    <row><cell>Event syntax</cell><cell>eventE(X) :> body.</cell><cell>eventE(X) :> body. (identical!)</cell></row>
    <row><cell>Present events</cell><cell>Atomic (en/1)</cell><cell>Atomic (body only, no :- definition)</cell></row>
    <row><cell>Multi-events</cell><cell>deltat/1 time window</cell><cell>within(Seconds) in event list</cell></row>
    <row><cell>Tell/Told</cell><cell>Body conditions supported</cell><cell>Body conditions supported (identical!)</cell></row>
    <row><cell>Send message</cell><cell>messageA(dest, send_message(...))</cell><cell>Same, or send(dest, content)</cell></row>
  </table>
</slide>

<slide type="table">
  <title>DALI vs DALI2 — New Features</title>
  <text>Features available in DALI2 that have no DALI equivalent:</text>
  <table>
    <header><cell>Feature</cell><cell>DALI</cell><cell>DALI2</cell></header>
    <row><cell>Periodic tasks</cell><cell>—</cell><cell>every(seconds, goal).</cell></row>
    <row><cell>Condition monitors</cell><cell>—</cell><cell>when(condition) :- body.</cell></row>
    <row><cell>Helpers</cell><cell>—</cell><cell>helper(head) :- body.</cell></row>
    <row><cell>Proposal handler</cell><cell>propose + call_propose</cell><cell>on_proposal(action) :- body.</cell></row>
    <row><cell>Learning</cell><cell>—</cell><cell>learn_from(event, outcome) :- cond.</cell></row>
    <row><cell>AI Oracle</cell><cell>—</cell><cell>ask_ai(context, result)</cell></row>
    <row><cell>Blackboard</cell><cell>Linda (TCP)</cell><cell>bb_read/bb_write/bb_remove (Redis)</cell></row>
    <row><cell>Ontology</cell><cell>meta/3 + OWL</cell><cell>ontology(same_as(a,b)).</cell></row>
  </table>
</slide>

<slide type="content">
  <title>Web UI and REST API</title>
  <text>Integrated web interface at http://localhost:8080 with full REST API.</text>
  <bullets>
    <b>**Agent list** — local and remote agents with running/stopped status</b>
    <b>**Event log** — real-time log with filtering by agent</b>
    <b>**Send events** — inject events into any agent from the browser</b>
    <b>**Agent details** — beliefs, past events, goals, start/stop controls</b>
    <b>**Blackboard viewer** — current shared blackboard state</b>
    <b>**Source editor** — edit and hot-reload agent definitions (double-click logo)</b>
    <b>**Cluster panel** — view all agents across all nodes in the Redis cluster</b>
    <b>**AI Oracle panel** — configure API key, model, and test AI queries</b>
    <b>**REST API** — full programmatic access: /api/agents, /api/send, /api/beliefs, /api/logs, etc.</b>
  </bullets>
</slide>

<slide type="table">
  <title>REST API Reference</title>
  <text>All endpoints available at http://localhost:8080/api/</text>
  <table>
    <header><cell>Method</cell><cell>Endpoint</cell><cell>Description</cell></header>
    <row><cell>GET</cell><cell>/api/status</cell><cell>System status</cell></row>
    <row><cell>GET</cell><cell>/api/agents</cell><cell>List agents with status</cell></row>
    <row><cell>POST</cell><cell>/api/send</cell><cell>Send event {"to":"agent","content":"event(args)"}</cell></row>
    <row><cell>POST</cell><cell>/api/inject</cell><cell>Inject event {"agent":"name","event":"event(args)"}</cell></row>
    <row><cell>GET</cell><cell>/api/beliefs?agent=X</cell><cell>Get agent beliefs</cell></row>
    <row><cell>GET</cell><cell>/api/past?agent=X</cell><cell>Get past events</cell></row>
    <row><cell>GET</cell><cell>/api/learned?agent=X</cell><cell>Get learned patterns</cell></row>
    <row><cell>GET</cell><cell>/api/blackboard</cell><cell>View blackboard tuples</cell></row>
    <row><cell>POST</cell><cell>/api/ai/ask</cell><cell>Query AI {"context":"..."}</cell></row>
    <row><cell>GET</cell><cell>/api/cluster</cell><cell>All agents across all nodes</cell></row>
  </table>
</slide>

<!-- ═══════════════════════════════════════════════════════════
     CLOSING
     ═══════════════════════════════════════════════════════════ -->

<slide type="end">
  <title>Thank You</title>
  <subtitle>DALI2 — Multi-Agent System Framework
University of L'Aquila — DISIM

Documentation: RULES.md (language reference) · EXAMPLES.md (examples guide)
Source: github.com/AAAI-DISIM-UnivAQ/DALI2</subtitle>
</slide>

</presentation>
